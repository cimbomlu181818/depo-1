<Window x:Class="DepoStok.ImportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="İçe aktar"
        Height="520" Width="600"
        MinHeight="400" MinWidth="500"
        WindowStartupLocation="CenterOwner">

    <DockPanel Margin="15">

        <!-- ALT: DÜĞMELER -->
        <StackPanel DockPanel.Dock="Bottom"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,15,0,0">
            <Button x:Name="ImportButton"
                    Content="İçe Aktar"
                    Width="100" Height="30"
                    IsEnabled="False"
                    Click="ImportButton_Click" />
            <Button Content="Kapat"
                    Width="90" Height="30"
                    Margin="8,0,0,0"
                    Click="CloseButton_Click" />
        </StackPanel>

        <!-- ÜST: DOSYA SEÇİMİ -->
        <DockPanel DockPanel.Dock="Top" Margin="0,0,0,10">
            <Button DockPanel.Dock="Right"
                    Content="Dosya seç..."
                    Width="100"
                    Margin="8,0,0,0"
                    Click="ChooseFileButton_Click" />
            <TextBox x:Name="FilePathBox"
                     Height="28"
                     IsReadOnly="True"
                     VerticalContentAlignment="Center" />
        </DockPanel>

        <!-- ÖZET / EŞLEŞME BİLGİSİ -->
        <TextBlock x:Name="SummaryText"
                   DockPanel.Dock="Top"
                   TextWrapping="Wrap"
                   Margin="0,0,0,10" />

        <!-- SONUÇLAR (hatalı satırlar) -->
        <TextBlock DockPanel.Dock="Top"
                   Text="Atlanan satırlar"
                   FontWeight="SemiBold"
                   Margin="0,0,0,5" />

        <ListBox x:Name="ErrorList" />

    </DockPanel>
</Window>
